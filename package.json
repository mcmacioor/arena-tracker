{
  "name": "arenatracker",
  "version": "0.3.0",
  "private": true,
  "description": "Local ArenaTracker app with optional Riot API sync.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js",
    "smoke": "node work\\smoke-test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
